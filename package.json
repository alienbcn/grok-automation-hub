{
  "name": "grok-automation-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Grok Automation Hub — official Playwright MCP over HTTP",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node scripts/start.mjs",
    "browsers": "npx playwright install chromium",
    "test:todomvc": "node tests/todomvc.smoke.mjs"
  },
  "dependencies": {
    "@playwright/mcp": "latest"
  }
}
